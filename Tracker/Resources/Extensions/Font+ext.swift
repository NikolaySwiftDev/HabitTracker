
import SwiftUI

extension Font {
    
    static func fontSystem(size: CGFloat, weight: Font.Weight) -> Font {
        let font = Font.system(size: size, weight: weight)
        return font
    }
    
}
